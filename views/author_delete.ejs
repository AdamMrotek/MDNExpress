<!DOCTYPE html>
<html>
  </body> 
    <%- include('./partials/head') %>
    <div class="main-content">
      <%- include('./partials/nav') %>
      <div class="row">

        <div class="col-sm-10">         
          <h1>Author: <%= author.name %> </h1>
          <p><%= author.lifespan %></p>

            <h4>Books</h4>
            <% if(author_books.length >0){%>
              <p> Delete the following books before attempting to delete this author. </p>
              <div style="margin-left:20px; margin-top:20px">
                <% author_books.forEach(book =>  { %> 
                  <dl>
                    <a href="<%=book.url%>"><%=book.title %> </a>
                    <dd><%=book.summary%></dd>    
                  </dl>
                <%})%> 
              </div>
            <% }else{ %>  
              <p>Do you really want to delete this Author?</p>
              <form action="" method="post">
                <div class="form-group">
                  <input type="hidden" id="authorid" class="form-control"type='hidden' name='authorid' required='true' value='<%=author._id%>' >
                  <button class="btn btn-primary" type='submit'>Delete</button>
                </div>
              </form>
            <% } %> 
          </div>
      </div>
    </div> 
  </body>
</html>